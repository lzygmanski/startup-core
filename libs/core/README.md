# Core Libraries

This directory is reserved for generic, reusable platform libraries:

- `domain/` — pure shared concepts only.
- `application/` — generic use cases and ports.
- `contracts/` — stable DTOs and public interfaces.

Do not add product-specific behavior here. Phase 3 introduces the first core API libraries.
