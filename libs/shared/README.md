# Shared Libraries

Place generic cross-cutting code here when it has more than one consumer. Future categories include UI, configuration, testing, authentication, and observability. Keep each library narrowly scoped and tagged in Nx.
