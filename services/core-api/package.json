{
  "name": "@startup-core/core-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.service.json",
    "test": "vitest run --config vitest.config.ts"
  }
}
